Re-apply patch 2: replace MPASS with NULL check in pt_cpu_start.

--- a/sys/amd64/pt/pt.c
+++ b/sys/amd64/pt/pt.c
@@ -257,7 +257,8 @@
 	struct pt_cpu *cpu;

 	cpu = &pt_pcpu[curcpu];
-	MPASS(cpu->ctx != NULL);
+	if (cpu->ctx == NULL)
+		return;

 	dprintf("%s: curcpu %d\n", __func__, curcpu);
 	pt_cpu_set_state(curcpu, PT_ACTIVE);
